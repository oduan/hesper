{
  "name": "@hesper/app-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "pnpm --filter @hesper/shared build && pnpm --filter @hesper/persistence build && tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @hesper/shared build && pnpm --filter @hesper/persistence build && tsc -p tsconfig.json --noEmit",
    "test": "vitest --run"
  },
  "dependencies": {
    "@hesper/persistence": "workspace:*",
    "@hesper/shared": "workspace:*"
  },
  "devDependencies": {}
}
